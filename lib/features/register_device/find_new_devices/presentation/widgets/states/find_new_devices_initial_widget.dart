import 'package:flutter/material.dart';

class FindNewDevicesInitialWidget extends StatelessWidget {
  const FindNewDevicesInitialWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
